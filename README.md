# TikTok News Workflow

Development repository for the modular TikTok News Studio workflow concept.

The goal is a simple, Make-style production flow in which every workflow item is an isolated module with a clear input/output contract.

> Active development happens on a dedicated development branch. `main` is kept as the stable baseline.
